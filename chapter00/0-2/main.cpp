/*
0-2.Write a program that, when run, writes£ºThis (") is a quote, and this (\) is a backslash
*/
#include <iostream>
using std::cout;using std::endl;
int main()
{
	cout<<"This (\") is a quote,and this (\\) is a backslash"<<endl;
	return 0;
}