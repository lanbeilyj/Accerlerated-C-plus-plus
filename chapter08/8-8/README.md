Becasuse the iterator do not support one iterator plus another iterator,
and end-begin, its' type is called difference_type( an signed int),
iterator allow this operation:
```
iter+n,
iter-n,
iter1-iter2,
```
