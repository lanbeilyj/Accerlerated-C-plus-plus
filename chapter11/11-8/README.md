Analysis:
vector:
```
storage structure: sequential;
elements access: using index(random)
only support: push_back()
iterator type: forward iterator(++)
```
list
```
storage structure: chain;
elements access: using iterator
only support: push_back(),push_front() insert() .etc
iterator type: Bidirectional iterator(++/--)

```

